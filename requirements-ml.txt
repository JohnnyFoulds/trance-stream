# ML-based audio analysis tools (large downloads, separate from dev)
# First pip install downloads ~2GB PyTorch; Demucs model ~1GB on first run
# basic-pitch model ~50MB on first run
demucs>=4.0.0
basic-pitch>=0.3.0
torchcodec       # required by torchaudio for WAV save in demucs 4.0+
pretty_midi>=0.2.11
